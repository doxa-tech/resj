<% content_for :id, 'articles' %>
<div class="top">
	<h1>Articles</h1>
</div>
<%= render "resource_goback" %>

<% @articles.each do |article| %>
<div class="article">
	<h2><%= article.title %></h2>
	<%= content_tag :div, (image_tag article.image), class: "img" %>
	<%= content_tag :div, (simple_format(article.content)), class: "content"  %>
	<div class="end"><%= l article.created_at %></div>
</div>
<% end %>