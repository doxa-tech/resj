<%= render layout: 'form' do |f| %>

	<div class="block">
		<div class="label">
			<!-- 	<%= f.label :email %> -->
			<p>
				<span class="num">7</span> Un petit <span class="b">email</span> de <span class="b">contact</span> public si vous le désirez (affiché publiquement)
			</p>
		</div>
	<%= f.text_field :email, placeholder: "contact@mail.ch" %>
	</div>

	<div class="clear"></div>

	<div class="block team">
		<div class="label">
			<p>
				<span class="num">8</span> Qui sont les <span class="b">responsables</span> du groupe? Il faut marquer un responsable comme admin. C'est lui qui sera le référent auprès de ResJ et qui aura le compte administrateur de la carte. Il est possible pour tous les responsables d'avoir leur propre compte sur ResJ afin d'accéder aux sections réservées (plus d'infos). <br>Nous contrôlons automatiquement si un utilisateur avec une adresse e-mail identique existe déjà sur ResJ. Dans ce cas nous associons son compte avec votre groupe. Celui-ci devra accepter une invitation envoyée automatiquement
			</p>
		</div>
		<div class="association_fields autocomplete" data-link="responsables">
			<%= f.fields_for(:responsables, @card.responsables.any? || @card.responsables.build) do |responsables_fields| %>
				<% is_contact = (responsables_fields.object.is_contact == "true") %>
				<%= content_tag :div, class: "fields" + (" contact" if is_contact).to_s do %>
					<div class="field">
						<%= responsables_fields.label :firstname %>
						<%= responsables_fields.text_field :firstname, data: {attr: "firstname"} %>
					</div>
					<div class="field">
						<%= responsables_fields.label :lastname %>
						<%= responsables_fields.text_field :lastname, data: {attr: "lastname"} %>
					</div>
					<div class="field">
						<%= responsables_fields.label :email %>
						<%= responsables_fields.text_field :email %>
					</div>
					<div class="field">
						<%= responsables_fields.hidden_field :_destroy, identifier: "destroy" %>
						<%= link_to_remove_fields "Supprimer" %>
						<%= responsables_fields.hidden_field :is_contact, identifier: "is_contact", value: ("true" if is_contact) %>
						<%= link_to_add_contact "Marquer comme propriétaire" unless is_contact %>
					</div>
				<% end %>
			<% end %>
			<%= link_to_add_fields("Ajouter un responsable") %>
		</div>
	</div>

<% end %>

<script type="text/javascript">window.onload=autocomplete();</script>