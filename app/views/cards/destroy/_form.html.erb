<%= form_tag nil, method: :delete, remote: true do %>

  <div class="errors"></div>

  <p>Pour confirmer la suppression, veuillez entrer le nom du groupe (<%= @card.name %>):</p>

  <%= text_field_tag :card_name %>

  <%= submit_tag "Supprimer" %>

<% end %>
