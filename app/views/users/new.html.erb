<% content_for :id, 'user-new' %>

<div class="top">
	<h1>S'enregistrer sur RéseauJeunesse</h1>
</div>

<div class="form">
	<%= form_for @user, url: users_path do |f| %>
		<%= render 'form', f: f %>
		<%= f.submit %>
	<% end %>
</div>