<% content_for :id, "profile" %>

<div class="col split1">
	<div class="block">
		<div class="header">
			<h2>Votre compte sur ResJ</h2>
		</div>
		<ul>
			<li class="avatar">
				<a href="<%= profile_path %>"><%= image_tag image_url current_user.gravatar_url %></a>
			</li>
			<li>
				<%= "#{current_user.firstname} #{current_user.lastname}" %> <%= render 'application/admin/edit_btn', path: user_edit_path %>
			</li>
			<li>
				Mes groupes, oeuvres ou réseaux <%= render 'application/admin/edit_btn', path: user_my_cards_path %>
			</li>
			<li>
				Avatar  <%= render 'application/admin/edit_btn', path: user_avatar_path %>
			</li>
		</ul>
	</div>
</div>