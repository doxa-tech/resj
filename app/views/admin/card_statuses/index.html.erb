<h1><%= t('card_status.admin.index') %></h1>

<p>Rechercher un statut</p>
<%= render 'application/admin/search_field' %>

<div id="admin_table">
	<div class="table_buttons">
		<% if current_permission.allow_modify?(params[:controller], "update") %>
			<a class="edit off">Editer</a>
			<a class="edit on" href="#">Editer</a>
		<% end %>
	</div>
	<%= present @table %>
</div>